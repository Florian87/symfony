{% extends "TroiswaBackBundle::layout.html.twig" %}

{% form_theme formulaire _self %}

{% block form_errors -%}
    {% if errors|length > 0 -%}
        {%- for error in errors -%}
            <p>{{ error.message }}</p>
        {%- endfor -%}
    {%- endif %}
{%- endblock form_errors %}

{% block content %}

       <div class="row">
        <div class="col-lg-12">

				{{ form_start(formulaire,{"attr":{"novalidate":"novalidate"}}) }}

				{{form_label(formulaire.prenom,"Votre prénom",{"label_attr":{"class":"form-control"}})}}

				{{form_widget(formulaire.prenom,{"attr":{"class":"form-control"}})}}

				{{form_errors(formulaire.prenom)}}

				{{form_label(formulaire.nom,"Votre nom",{"label_attr":{"class":"form-control"}})}}

				{{form_widget(formulaire.nom,{"attr":{"class":"form-control"}})}}

				{{form_errors(formulaire.nom)}}

				{{form_label(formulaire.sexe,"Votre sexe",{"label_attr":{"class":"form-control"}})}}

				{{form_widget(formulaire.sexe,{"attr":{"class":"form-control"}})}}

				{{form_errors(formulaire.sexe)}}

				{{form_label(formulaire.biographie,"Votre biographie",{"label_attr":{"class":"form-control"}})}}

				{{form_widget(formulaire.biographie,{"attr":{"class":"form-control"}})}}

				{{form_errors(formulaire.biographie)}}

				{{form_label(formulaire.dateNaissance,"Votre date de naissance",{"label_attr":{"class":"form-control"}})}}

				{{form_widget(formulaire.dateNaissance,{"attr":{"class":"form-control"}})}}

				{{form_errors(formulaire.dateNaissance)}}

				{{ form_end(formulaire) }}0

        </div>
    </div>

{% endblock %}